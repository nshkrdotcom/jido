# Automated release workflow using central reusable workflows
# Trigger manually via GitHub Actions UI

name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Release
    uses: agentjido/github-actions/.github/workflows/elixir-release.yml@v1
    secrets: inherit
